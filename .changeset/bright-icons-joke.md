---
"vite-plugin-caddy-multiple-tls": minor
---

Export `resolveCaddyTlsDomains()` and `resolveCaddyTlsUrl()` so external tooling can compute the same resolved local HTTPS domains and URL as the plugin.
