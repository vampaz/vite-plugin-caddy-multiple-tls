{
  "name": "vite-plugin-caddy-multiple-tls-e2e",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "playwright test",
    "test:combinations": "./run-combinations.sh",
    "test:preview": "playwright test --config playwright.preview.config.ts",
    "test:all": "npm run test && npm run test:preview"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.1"
  }
}
